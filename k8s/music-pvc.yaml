apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rosetta-music-pvc
  namespace: rosetta
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: rosetta-music-pv
