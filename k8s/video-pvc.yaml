apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rosetta-video-pvc
  namespace: rosetta
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 2Gi
  volumeName: rosetta-video-pv
